# MiniRPG 产物目录

> 这里放不参与项目运行时加载、但需要跟项目一起留存的产物文件。

---

## 当前目录

```text
Artifacts/
└── Recovery/
    └── .gitkeep
```

## 规则

- `Artifacts/` 不放场景、脚本、运行时资源或数据表。
- `Recovery/` 只放人工保留的恢复快照、临时备份这类非运行时文件。
- 当前工作区里没有保留中的恢复快照；后续如果需要留档，统一放进 `Recovery/`。
- 如果某个产物只为本地一次性排障服务，优先移出仓库，而不是长期堆在项目根目录。
