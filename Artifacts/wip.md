# WIP 文件承包

本文件是多 AI / 进程协作时的"正在动的文件"登记表。
开工前扫一遍，别碰不属于你这块的；动你这块之外的文件前，在这里登记。

## 格式

```
- <进程名> | <文件/目录 glob> | <预计完成时间 / 当前状态>
```

## 当前登记

<!-- 按开工时间倒序。完成后删除本条。 -->

- cursor-opus-mcp5-fx-p1 | 素材需求清单 §3 B1 战斗特效 7 张：走 `Tools/banana/banana_gen.py` 落地到 `Assets/Art/Placeholders/effects/fx_{arrow_projectile,buff_flash,hit_blunt,lightning_strike,pickup_glint,poison_spit,slash_arc}.png`（只换 png，**不动** `Data/combat_fx.json` 与 `Module/Render/CombatFxPlayer.cs`，和 mcp8 的 M7 修复互不干扰）；中转 `sg2c.dchai.cn` 的 `Nano_Banana_2_2K_0` 流式返回 4MB 图常 `IncompleteRead`，顺带给 `Tools/banana/banana_gen.py::_openai_chat_completion` 加 `urllib.error.URLError` / `http.client.IncompleteRead` 自动重试（指数退避最多 3 次，默认打开，不改签名、不影响 gemini 原生协议路径）。 | 进行中 2026-04-20
- qtwx-mcp-3-inv-drag-fix | `Module/Panel/InventoryGridPanelModule.cs`（背包网格拖不动修复：placement 换 DragDropControl + press 不 Refresh，已改；收口清过期 SetDragForwarding 注释）、`Docs/界面与面板.md`（InventoryPanel 条目按"操作&反馈"模板重写 + Chest/Trade/Conversation/ActorInspect/SkillBar 占位 TODO）、`Module/Panel/README.md`（补 Godot 4 drag-drop 三条常识 + Refresh 节奏契约）、`Artifacts/scan_2026_04_19/*`（7 份合并归档）。不动别人在动的 Revival/Log/Demographics/Thoughts/Data。 | 进行中 2026-04-20

## 已完成（最近）

<!--
本节定期清空，避免遮住"现在没人在动什么"。
旧条目按归档日期挪到 Artifacts/wip_archive_<日期>.md，需追溯历史归因时按需查阅。
最近一次归档：[Artifacts/wip_archive_2026-04-19.md](wip_archive_2026-04-19.md)
（覆盖 2026-04-17 至 2026-04-19 全部已完成条目）
-->

- qtwx-mcp-7-orphan-cleanup | 2026-04-20 完成。孤儿代码清理（承接 `Artifacts/全量巡检_2026-04-20.md` 之后的独立巡检）。决策标尺 0：**Y**（删 -33KB 死码后，下一位 AI 读 `Docs/架构现状.md` 边界列表时不被"刚写完就被绕过的 `SurfaceFreeMoveTraversal`"、"0 caller 的 `NoopLocalServerLauncher`"、"错挂 `JsonDerivedType` 的 `ITagSourcePolymorphic`"、"Dialog 旧系统残留 `DialogSet/Entry/Option`"等误导，每减噪一分就减少一分错改风险）。**⚠ 已代 `cursor-opus-mcp9-audit-phase2` 做完 `SurfaceFreeMoveTraversal.cs` 的"删或接入"决断——选"删"**（全仓 0 代码 caller，注释声称"给 ActionModule 和 AutoNavigationPlanner 共用"但两者均未接入）；`Data/Config/demographics_runtime.json` 的决断未动，仍留给 mcp9。改动：①**删整文件 7 个** —— `MiniRPG.Shared/Core/World/SurfaceFreeMoveTraversal.cs`、`Module/Network/NoopLocalServerLauncher.cs`（运行时统一 `LocalProcessServerLauncher`）、`Module/Panel/PanelTransition.cs`（UI helper 无 using）、`MiniRPG.Shared/Core/World/BlockPlaceModule.cs` + `Tests/MiniRPG.Tests/PlacementConnectivityTests.cs`（建造现走 `FacilityConstructionModule`）、`MiniRPG.Shared/Core/Event/DailySummaryBuilder.cs` + `Tests/MiniRPG.Tests/DailySummaryBuilderTests.cs`（`DailySummary` 对象无 HUD/面板消费者）；②**文件内删类** —— `PresetDB.cs` 删 `private interface IHasId`（`GetId` 用反射读 "Id"）、`TagSystem.cs` 删 `ITagSourcePolymorphic` + 5 个错挂的 `[JsonDerivedType]`（挂在无实现子接口，System.Text.Json 永读不到）、`DialogTypes.cs` 删 `DialogSet/Entry/Option`（保留 `DialogCondition`/`DialogEffect`，仍被 `ConversationModule:240` + `DialogConditionMatcher` 使用）；③**轻改注释** `CalendarServiceTests.cs` 移除 `DailySummaryBuilder` 的 `<see cref>` 引用。**构建**：`dotnet build MiniRPG.csproj` 0 错 0 警。**测试**：过滤 `PortraitComposer` / `FoodConsumeFreshness`（归档已记录的 pre-existing native crash / 断言漂移，与本轮无关）后 **1379/1379 通过**。完整扫描数据落盘 `Artifacts/orphan_scan_raw.csv`（1049 个类型的全仓引用计数），供后续增量扫描复用。
- qtwx-mcp-10-bystander-grief | 2026-04-20 完成。补愿景"死亡仪式感第 3 条：旁观者挂 thought"。决策标尺 0：Y——死亡场景的代入感直接增强：愿景明文规定"其他队员 / 附近 NPC 在死亡的那一刻要各获得一条 thought"，之前除亲属外所有目击者 mood 无反应。新增 `MiniRPG.Shared/Core/Social/BystanderGriefCapturer.cs`（IGameEventConsequenceHandler，同 Z 层 Chebyshev<=8 扫描旁观者，排除受害者/凶手/已死/亲属列表里的 survivor，按是否同 Party 发 `ally_died` 或 `witnessed_death` thought）；`Data/thoughts.json` 增两条（`witnessed_death` -6/240 turn；`ally_died` -10/480 turn）；`Data/I18n/{en,zh_CN}.json` 补两 key（中："目睹了一场死亡" / "同伴倒下了"；英："Witnessed a death" / "A companion fell"）；`App/Main.Startup.cs` 在 KinshipLossCapturer 之后加一行 Register；`MiniRPG.Shared/Core/Multiplayer/ServerSideConsequenceDispatcher.cs` 对称加 `BystanderGrief` 属性 + Register；`Tests/MiniRPG.Tests/BystanderGriefCapturerTests.cs` 新增 7 case（范围、跨 Z、凶手豁免、亲属优先、非死亡事件、3 种 health death cause、死者豁免）；同步改 `ServerSideConsequenceDispatcherTests` / `RoomRuntimeHostConsequencePipelineTests` 的 HandlerCount 5→6。**构建**：`dotnet build` 0 错 0 警。**测试**：新增 7/7 通过；KinshipLoss + ActorMemoryModule + DeathReport + ConsequenceRouter + ActiveActorDeath + PlayerDeathPresenter 64/64 通过；过滤掉其他进程遗留的 PortraitComposer / FoodConsumeFreshness 预先失败后，全量 1379/1379 通过。
- cursor-opus-mcp9-audit-phase2 | 巡检 phase2（H1 + M9 + 愿景仪式感 3/4 补接线）。决策标尺 0：Y（**愿景仪式感第 1-4 条全部至少数据层 + 日志层兑现**——玩家现在能在死亡-复活流程里看到：①死亡切换演出 heading（phase1）、②仪式按回合推进 + 剩余回合日志、③旁观 NPC 自动获得 thought（mcp10 落地，本轮确认）、④复活失败落残魂 + toast + 日志。这直接把"结果可追溯 + 代入感"从数据层推到 UX 层；往可玩闭环近一步）。**本轮改动**：①`MiniRPG.Shared/Core/Combat/TimelineTurnManager.cs::TryExecuteRevive` 从 `corpse.Corpse?.SourceActorId` 查回原 actor，给 `revival_ritual_started` / `revival_ritual_progress` 事件调 `IdentificationModule.PopulateTargetIdentity` 填 {target}；②`Module/LogModule.cs` 新增 `revival_ritual_started` / `revival_ritual_progress` switch 分支（用已存在的 `log.revival.ritual_started` / `log.revival.ritual_progress` i18n key）；③`Data/I18n/{zh_CN,en}.json` 补 `log.revival.soul_fragment_dropped` + `toast.revival_soul_fragment_dropped` 两条 key（填 LogModule 已引用但 i18n 缺失的孤儿）；④`App/RuntimeUi/GameEventPresentationRouter.cs` 加 `case "revival_soul_fragment_dropped"`，调 `_ground.Invalidate()` + 发一条 warning toast 让玩家立刻看到"一块 X 掉落"；⑤**删**孤儿文件 `Data/Config/demographics_runtime.json`（0 caller，字段与 `conception_defaults.json` 重叠但值不一致——保留只会让运维改错文件，不读的文件先删干净）。**依赖 / 协作确认**：H1 `revival_failed` + `conversation_opened/closed/observer_joined/takeover_*` 翻译已由其他进程在 LogModule 落地（`Ardynox` / 另一 cursor-opus instance，非我），`revival_soul_fragment_dropped` switch 分支也是其他进程加的（未提交），本轮只补 i18n key + toast。M1 `SurfaceFreeMoveTraversal.cs` 已由 `qtwx-mcp-7-orphan-cleanup` 删除；愿景仪式感第 3 条（旁观反应 thought）已由 `qtwx-mcp-10-bystander-grief` 完整落地；愿景仪式感第 4 条（失败残魂 item drop）已由其他进程在 `ReviveService.cs` 落地 + 加 3 个覆盖测试（`TryRevive_PermanentlyLost_WithWorld_DropsSoulFragmentAtReviverFeet` / `TryRevive_SpellMisfire_WithWorld_DropsSoulFragmentAndConsumesHalfMaterials` / `TryRevive_InsufficientMaterials_DoesNotDropSoulFragment`）。**构建**：`dotnet build mini-rpg.sln` 0 错 0 警。**测试**：`Revive|DeathReport|PlayerDeathPresenter|Bystander|TimelineTurn` 过滤 59/59 通过。

- cursor-opus-mcp9-audit-patch | `Artifacts/全量巡检_2026-04-20.md` 速通清单 6 条已清（+ Gemini 文案）：决策标尺 0：Y（**H2 + Δ-8** 直接让中文玩家死亡报告面板从"英文 fallback + 双 heading"恢复到"中文标准 + 单 heading"，兑现愿景"结果可追溯"的核心 UX；**H5** 让 AutoTest 冒烟在 Tab 切焦点后断言正确、保护 CI 基座；**M11 + L4/L5** 清编译警告和文档漂移，降低下一位协作者的认知成本）。具体改动：①`Data/I18n/zh_CN.json` + `en.json` 补 10 条 death.* key（death.party_wiped / death.report.cause / death.report.recent_count / death.cause.{killed_by,killed,incapacitated,blood_loss,infection,food_poisoning,unknown}），文案经 Gemini subagent 审美打磨（中文去翻译腔："战斗毙命" / "感染身亡" / "全队覆灭"，英文 PZ clinical 风）；②`App/Main.Timeline.cs::HandlePlayerDeath` 删除 Main 内的 heading 日志（原本用 ternary 只分 killed/incapacitated，party_wiped 落回 fallback "death.killed"），把 `_playerDeathPresenter.Present` 调用上移到 turn/floor/kills 详情行之前，让 Presenter 统一写 heading + 死因 + 最近事件计数；③`App/Main.AutoTest.cs::CaptureAutoTestSnapshot` `ActorModule.GetPlayer(_state)` → `ActiveActorAccess.GetActive(_state)`，groundItemCount 一起切到 active actor 的坐标（Tab 切焦点后断言指向当前受控角色而非队长）；④`Tests/MiniRPG.Tests/DemographicsBabyFoodTests.cs` 4 处 `default` → `default!`（消除 CS8625）；⑤`Data/README.md` 删 `dialogs.json`、改写为 `Conversations/*.conversation.json`；⑥`Docs/会话生命周期.md` `_dialogPanel` → `_conversationPanel`。**跳过**：M4（审计误报——`Scene/DialogPanel.tscn` 被 `ConversationPanelModule` 复用，`App/Main.Panels.cs:45` 载入此 scene 作为 ConversationPanel 骨架，非死资源）。**构建**：`dotnet build mini-rpg.sln` 0 错误 0 警告（上次 4 条 CS8625 已清）。**测试**：`DeathReport/PlayerDeathPresenter/Revive/Demographics` 过滤后 73/73 通过；整套 1117 个测试中有 2 个预先失败（`FoodConsumeFreshnessTests.TryConsumeFood_Rotten...` 断言漂移 20 vs 12.2、`PortraitComposerTests.Clear_Purges...` Godot.Image 原生崩溃 AccessViolation），均属于其他进程 untracked 新测试，不在本轮改动范围内。
