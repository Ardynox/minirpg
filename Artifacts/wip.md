# WIP 文件承包

本文件是多 AI / 进程协作时的"正在动的文件"登记表。
开工前扫一遍，别碰不属于你这块的；动你这块之外的文件前，在这里登记。

## 格式

```
- <进程名> | <文件/目录 glob> | <预计完成时间 / 当前状态>
```

## 当前登记

<!-- 按开工时间倒序。完成后删除本条。 -->

- qtwx-mcp-9-water-subview | `Scene/WaterPainterlyBlockSubview.{cs,tscn}`(新) `Scene/WaterPainterlyBlockSubviewDebug.{cs,tscn}`(新 iso 调试场景) | 进行中 2026-04-20 V1 完成：把 `WaterPainterlyBlockPreview` 的水块包进 `SubViewportContainer + SubViewport`，相机按游戏 2:1 等距（yaw 45°、pitch 30°、正交）、自动 fit grid，只渲染 +X/+Z 两个可见侧面，砍 boat/drop/debug-UI/mouse-input，[Export] block/grid/viewport/相机参数，公开 `SpawnRipple/SpawnRippleQuiet/SpawnWakeRipple/ClearRipples/GetViewportTexture`。Debug 场景用 `IsoCoordUtil` 画 5×5 的 iso ground tile（菱形顶 + 两片侧面 parallelogram），中心一格换成 WaterPainterlyBlockSubview（128×128），鼠标点水 = 生成 ripple，[/]调 amp、F 切 splash、C 切背景、R 清 ripple、滚轮 zoom、中键 pan。`dotnet build` 0 错 0 警。**原 `WaterPainterlyBlockPreview.cs` 本轮未动**（V1 先走重复代码让 subview 能独立跑通确认方向，V2 再抽 shared core 给 Preview 和 Subview 共用，避免一次改 1800 行带来回归风险）。不动别的 Water*Preview / 主渲染 / IsometricVoxelRenderer
- qtwx-mcp-1-billbar | `Module/Panel/InlineBillBarModule.cs`(新) `Scene/InlineBillBar.tscn`(新) `MiniRPG.Shared/Module/ServerActionGateway.cs` `App/Main.UiMode.cs` `App/Main.Panels.cs` `Docs/界面与面板.md` | 进行中 2026-04-20
- qtwx-mcp-1-revive-facility | `Data/facilities.json` `Data/entity_render.json` `Data/I18n/{en,zh_CN}.json` `App/RuntimeUi/GameEventPresentationRouter.cs` | 进行中 2026-04-20
- qtwx-mcp-7-bg3-grass-preview | 2026-04-20 完成。仿 BG3 风格的独立 3D 草地预览场景。决策标尺 0：**N**（这是渲染技术 demo，不直接推进主游戏可玩闭环；价值在于（1）给项目补一个"3D spatial shader + MultiMesh 草叶 + 风摆/SSS"的可运行参考，未来做 CG 过场 / 非体素演示 / 宣传片草地时可复用；（2）类比 `Scene/GrassSurfacePreview.tscn` 的独立预览惯例，再加一个同格式的 3D 样例）。**产物**（4 新，全部独立，不改主项目任何文件）：①`Scene/BG3GrassPreview.tscn` 独立 Node3D 场景（WorldEnvironment 开 SDFGI/SSAO/Bloom/ACES + DirectionalLight3D 带 4 级 CSM 阴影 + Ground MeshInstance3D 脚本生成 mesh + Camera3D）；②`App/Previews/BG3GrassPreview.cs` 约 470 行（200×200 格 ArrayMesh 地形 fBm + 中心差分法线、40k 默认 MultiMesh 草叶 instance、orbit 相机 + WASD/QE pan、HUD + 11 组热键：密度 15K/40K/120K、风速风向、草高、太阳俯角、日/黄昏配色、种子 reroll、相机复位、HUD 切换）；③`App/Previews/bg3_grass_blade.gdshader` 3D spatial shader（INSTANCE_CUSTOM 4 通道传相位/色相/高度/朝向，顶端收窄 + 主频+阵风风摆 + 手写 light() 做 Lambert+SSS 背光透射，cull_disabled 叶片双面收光，EMISSION 兜底防全黑）；④`App/Previews/bg3_ground.gdshader` 3D spatial shader（fBm value noise 做色块扰动 + 高度带（泥土/草/干草）+ 坡度露土 + 距离蓝雾，normal 由地形顶点法线决定）。**F6 运行**：编辑器打开 `Scene/BG3GrassPreview.tscn` 按 F6 直跑，不依赖 GameState/SaveModule/TerrainRegistry/GrassCoverSampler 任何游戏系统。**构建**：`dotnet build MiniRPG.csproj` 0 错 0 警。**不动**：主项目 render 模块（`Module/Render/*`）、主场景（`App/Main.tscn`）、其他预览场景（`Scene/GrassSurfacePreview.tscn` / `Scene/Water*Preview.tscn`）、数据文件一概不碰。**已知取舍**：（a）草 mesh 不参与 SDFGI（`GIMode=Disabled`）、不投阴影（`CastShadow=Off`）—— 12 万 blade 开这俩项会严重掉帧，BG3 远景草也这么干；（b）120K 密度重建需 500ms-2s（C# 逐条 SetInstanceTransform），切密度时会短暂卡顿，可接受；（c）地形 200m×200m 为单片非 LOD，如果后续要放更大远景需要切 terrain chunk + blade LOD 两套系统。

## 已完成（最近）

- cursor-opus-b12-detail-mp | 2026-04-21 完成。身份卡 **B12**：新增 `Tools/banana/b12_detail_mp_fill_local.py`，落盘 `Placeholders/detail/` **20** 张 + `mp/` **8** 张占位；更新 `Artifacts/style_locks_B12.md`、`style_locks_B10.md`（标明 B10 为审美重刷）、`Artifacts/style_locks_registry.md`、`Artifacts/素材身份卡_运行时映射补充_2026-04-21.md`。决策标尺 0：**N**（资源就位、未接游戏逻辑；减少后续接线返工）。

- cursor-opus-b9-equipment-overlays | 2026-04-21 完成。**B9**：新增 `EquipmentOverlayPaths`（8 向路径 + `HasFull*Set`）；`MapSpriteRuntimeFactory` 在投影放大后按向叠 PNG，缺套则回退程序几何；`Clear()` 释放 overlay 图缓存。`Tools/banana/b9_equipment_overlay_fill_local.py` 写入 **88** 张 `Generated/equipment_overlays/` 占位（256²）。补全 `Artifacts/style_locks_B1.md`…`B12.md`（初版/登记）并刷新 `Artifacts/style_locks_registry.md`。`Assets/Art/README.md` 接入表补一行。`dotnet build MiniRPG.csproj` 0 错 0 警；`dotnet test` 在 xunit 进程末尾仍可能因 Godot `PortraitComposer` 非宿主环境偶发 **AccessViolation**（与本轮改动无关的已知限制）。决策标尺 0：**Y**（地图上装备三类在齐套 PNG 时走贴图管线，占位可换正式图）。

- cursor-opus-b4-faces-256 | 2026-04-21 完成。`Tools/banana/b4_faces_fill_local.py` 默认画布 **256×256**（`--size` 可改）；`python ... --force` 重写 **62/62** `Assets/Faces/**/*.png`，与身份卡 B4、`PortraitComposer` 画布一致。新增 `Artifacts/style_locks_B4.md`；更新 `Artifacts/style_locks_registry.md`（B4 状态 + Faces 快照）。决策标尺 0：**Y**（肖像合成走磁盘 PNG 时不再被 128 升采样糊掉；仍为土色占位可后续 Banana 替换）。

- cursor-opus-style-locks | 2026-04-21 完成。落实身份卡 §0.2：新增 `Artifacts/style_locks_B0.md`（全局锚点摘 `Assets/Art/README.md` / 需求清单 §0）、`Artifacts/style_locks_registry.md`（各批子锁登记表 + `Placeholders` 子目录 PNG 快照）、`Artifacts/素材身份卡_运行时映射补充_2026-04-21.md`（天气 id、`limb_*` 与身份卡 B7.4 命名差异）。决策标尺 0：**N**（不改玩法；收敛多通道发包与验收口径，服务后续 B4/B9/B10）。

- cursor-opus-item-world | 2026-04-21 完成。**接入**：`ItemWorldRenderRegistry.Load()`（`GameDataLocator` + 大小写不敏感 JSON）；`IsometricVoxelRenderer` 构造时加载注册表；`EntityDrawCommand` 携带地面物品 `templateId`/`category`/`stackIndex`，在实体绘制阶段优先 `TryDrawGroundItemWorldSprite`（`texture`/`catalog_texture` → `ResAccess.Get`，`tile` 仍跳过并回退圆点）；`EntityAccess.TryGetGroundItemRenderKeys`。**数据**：`item_world_render.json` 的 `default` + 九类 `categories` 改为 `res://Assets/Art/Placeholders/item_world/category_*.png`；`items` 覆盖仍多为 `catalog_texture`。**生成**：`Tools/banana/b2_item_world_driver.py`（banana 64px）；`b2_item_world_fill_local.py` 在 API 限额时用 PIL 写入 9 张土色块兜底（本轮已执行，磁盘已有 `category_*.png`）。`Assets/Art/README.md` §接入、`Docs/界面与面板.md` §3.3；测试扩 `ItemWorldRenderMapping_UsesSupportedKinds` + `Load_FromProjectData_ReturnsRegistryWithCategories`。`dotnet build` + `ItemWorldRenderRegistryTests` 6/6。决策标尺 0：**Y**（地图掉落物一眼可读；无图时行为与旧版一致）。

- cursor-opus-b1-weather-fx | 2026-04-21 完成。在本机跑 `b1_placeholders_driver.py`：`--only weather --skip-existing` 补 **snow_cover / ice_gloss / snow** 三张（与 `WeatherAssetIds` 对齐，此前仓库缺这 3 张）；`--only effect --skip-existing` 补 **`fx_explosion.png`** 根目录单图 fallback（多帧目录仍保留）。`--only branding --skip-existing` 已存在跳过。日志追加 `Tools/banana/.cache/b1_placeholders_driver.log`。决策标尺 0：**Y**（天气与爆炸缺资源时不再 silent 空引用）。

- cursor-opus-b8-wire-b1 | 2026-04-20 完成。**B8 接线**：`FarmModule` 的 `crop_planted` / `crop_mature` / `crop_withered` 事件写入 `GameEvent.ItemTypeId`（作物 def id）；`PlaceholderUiIconCatalog` 增加 `item_icons` / `crops` / `material_world` 路径与 `ResolveGroundItemIcon`；`IncidentAlertModule` 对成熟/枯萎显示 `crop_<id>_stage2` 占位图并新增枯萎通知文案；`GroundPanelModule` 行左 24px 图标（物品图优先，材料类 fallback `mat_*_stack`）。`FarmModuleTests` 断言 `ItemTypeId`；`Docs/界面与面板.md` §3.8 / §4.11 已补。**B1 生成**：新增 `Tools/banana/b1_placeholders_driver.py`（combat_fx 单图 fallback + `WeatherAssetIds` 天气贴图 + 主菜单品牌；日志 `Tools/banana/.cache/b1_placeholders_driver.log`）。`dotnet build` + `FarmModuleTests` 全绿。决策标尺 0：**Y**（HUD/地面列表/农事通知立刻吃到 B8 占位图；B1 脚本推进 P0 特效/天气/品牌替换）。

- cursor-opus-b8-banana | 2026-04-20 **B8 完结 45/45**（`b8_driver.log`：`ok=45 fail=0`，墙钟 ~5004s ≈1h23m）。`Assets/Art/Placeholders/crops/` **21**（7 作物 ×3 阶段）、`material_world/` **24**（`mat_<id>_stack`）。同轮已落地：B7 接线（状态肢体/能力 Tab、`LimbTarget` 行图标 + `PlaceholderUiIconCatalog` surgery/room/capacity/limb 路径）、`Docs/界面与面板.md`。**未接**：作物/材料图尚无游戏内引用点（农田 HUD 等后续再接）。决策标尺 0：**Y**。

- cursor-opus-b7-banana | 2026-04-20 **完结 39/39**。首轮 `ok=27 fail=12`（402 余额）；续跑 `b7_ui_icons_driver.py --only capacity,limb --skip-existing` 补满 capacity 4 + limb 多数；**最后 2 张**（`limb_right_leg`、`limb_heart_lungs`）在 banana 未出图前由 `Tools/banana/b7_ui_icons_fill_local.py` 本地 PIL 兜底写入（与 B7 同目录同尺寸，土色线框风）。日志 `Tools/banana/.cache/b7_driver.log`。决策标尺 0：**Y**。

- cursor-opus-b6-wire | 2026-04-20 完成。B6 占位图接入运行时 UI：`PlaceholderUiIconCatalog` 增加 `profession_*` / `race_*` / `interaction_*` / `incident_*` 路径方法；`SkillBarModule` 技能格有图则主显占位图+短状态；`SkillManagerModule` 列表行左 24px 图标；`CharacterCreationDialog` + `CharacterCreationModule` 种族/职业下拉旁 48px 预览；`IncidentAlertModule` 四类 incident 通知左侧 `incident_<InteractionDefId>` 图（与叙事 incident id 一致）。`Docs/界面与面板.md` §3.8 / §4.3 / §4.4 / 5.3 表已补。`dotnet build MiniRPG.csproj` 0 错 0 警。决策标尺 0：**Y**（玩家立刻看到职业/种族/技能/事件美术占位，可读性与代入感）。

- cursor-opus-b6-uiicons | 2026-04-20 完结。首轮 ~6.6h 得 **69/79**，缺 10 张；续跑 `--skip-existing --max-gen-retries 5` + `banana_gen` 超时 **720s** 约 **25min** 补全（`ok=10 skip=69 fail=0`）。**磁盘终态**：`profession` 14/14、`race` 12/12、`interaction` 43/43、`incident` 10/10，路径 `Assets/Art/Placeholders/ui_icons/` 合计 **79** 张。日志见 `Tools/banana/.cache/b6_driver.log`。决策标尺 0：**Y**。

- cursor-opus-status-needs-icons | 2026-04-20 完成。`StatusPanelModule` **Needs Tab** 纳入逐项模式：`IsPerItemTab` 含 `Needs`，`RebuildNeedsRows` 用 `HBox`（22px 图标 + 文案）；图标走 `PlaceholderUiIconCatalog.ResolveNeedRowIcon` / `ResolveMoodRowIcon` / `thought_*`（与 NeedsHud 一致）。`PlaceholderUiIconCatalog` 抽出上述 Resolve 方法；`NeedsHudModule` 改为调用 catalog；`ActorStatusTextBuilder.BuildNeedLine` / `BuildMoodLine` 改为 `internal` 供状态面板复用文案。`Docs/界面与面板.md` §4.1 + §9.6 已补。**未做**：Needs 行 hover tooltip（表 9.6 已标注）。`dotnet build MiniRPG.csproj` 0 错 0 警。决策标尺 0：**Y**（状态面板可读性与 HUD 一致）。

- cursor-opus-ui-icons-wire | 2026-04-20 完成。将 `Assets/Art/Placeholders/ui_icons/` 接入 **Needs HUD**（每行 `TextureRect` + 饥饿/渴/困阶段有 `thought_*` 则优先显示）与 **HealthAlerts**（每行 HBox：`Icon`+`Text`，条件/失血等有图；剧痛、湿透无对应 B5 图则隐藏图标）。新增 `Module/Panel/PlaceholderUiIconCatalog.cs`（路径 + `ResourceLoader` 缓存）。`Docs/界面与面板.md` §3.7/3.8 各补一句说明。**未做**：状态面板 Needs 纯文字 Tab 仍无图标（留作后续）。`dotnet build MiniRPG.csproj` 0 错 0 警。决策标尺 0：**Y**（HUD 可读性、代入感）。

- qtwx-mcp-10-asset-b5-uiicons | 2026-04-20 完成。B5 UI 图标批：`Tools/banana/b5_ui_icons_driver.py` 驱动 banana（OpenAI 中转）→ rembg → `crop_square_resize` 128×128；**51/51** 落 `Assets/Art/Placeholders/ui_icons/`（5 need + 12 condition + 34 thought；与 `needs.json` stage 重复的 need 档位未单独出图）。首轮批跑 2 张因 TLS 断连失败，已手工补跑并入库。决策标尺 0：**Y**（HUD/状态条有统一美术占位，直接改善可读性）。

- cursor-opus-mcp2-starter-kit | 2026-04-20 完成。创建角色界面「初始携带物品」功能落地。决策标尺 0：**Y**——让玩家在开局就能按职业偏好调整起始资源（猎人多带原料、商人多带温饱、debug 模式无视类上限给 QA / AutoTest 极端边界），把"我扮演的这个角色"代入感从"统一 9 件硬编码"推到"职业差异化 + 玩家可控"，直接推进愿景的"玩家掌控感 + 结果可追溯"。**改动 11 个文件**（4 新建 + 7 修改）：①新建 `MiniRPG.Shared/Core/Data/StarterKitCatalog.cs`（白名单 + 每类上限 + 通用 fallback 加载器，仿 `ItemSizeRegistry` 模式，JSON 缺失时内置 5 类 + 9 件包兜底，线程安全单写多读）；②新建 `MiniRPG.Shared/Core/Data/StarterKitResolver.cs`（三档解析：`Resolve` 优先 `options.StartingItems`、null 时 `ResolveDefault(professionId)` 走职业默认 → fallback；`Validate` 非 debug 严格按白名单 + 每类 limit 裁剪 + 单 entry PerEntryMaxCount=99 截断；debug 仅按 PerEntryMaxCount 截）；③新建 `Data/Config/starter_kit.json`（5 类 food/drink/rest/ingredient/tool + 各类上限 4/3/1/4/2 + 通用兜底 9 件包）；④新建 `Tests/MiniRPG.Tests/StarterKitResolverTests.cs`（16 case，覆盖职业默认 / 未知职业 / 玩家空清单 / 白名单过滤 / 类上限截断 / 重复合并 / 非法 itemId 丢弃 / 非正数丢弃 / PerEntryMaxCount / debug 绕过）；⑤改 `MiniRPG.Shared/Core/Data/PlayerCreationOptions.cs` 加 `StartingItems` (`IReadOnlyList<PlayerStartingItem>?`，null=用职业默认包，空列表=玩家明确选不带) + `StartingItemsDebugOverride` 字段，新增 `PlayerStartingItem` 类型；⑥改 `MiniRPG.Shared/Core/Data/PresetDB.cs` 给 `ProfessionPreset` 加 `StarterKit: Dictionary<string,int>` 字段；⑦改 `Data/professions.json` 给 7 个有特征的职业（hunter/cook/soldier/guard/merchant/miner/scout）配 starterKit，其他 7 个职业留空 → fallback（向后完全兼容）；⑧改 `MiniRPG.Shared/Core/Map/MapGenModule.cs::GiveStarterKit` 从硬编码 9 件改走 `StarterKitResolver.Resolve(options)` + 循环 `PresetDB.CloneItem`；⑨改 `Scene/CharacterCreationDialog.tscn` 在 Form 里 CustomizeRow 之后加 `StarterKitRow`（VBox = label + toolbar[reset btn + debug check] + dynamic list 容器）；⑩改 `Module/CharacterCreationModule.cs` ~200 行新代码：`BuildStarterKitRows` 按 catalog 分类铺 [−] N [+] 行（Label / Button 字典缓存）+ `RefreshStarterKitButtons` 按 categoryUsed 实时禁用 / 启用 + / − + dirty 标记策略（玩家未改过则切职业自动套新职业默认包）+ "恢复职业默认" 按钮 + debug checkbox 解除类上限 + `BuildStartingItems` 把 UI 状态喂给 `PlayerCreationOptions`；⑪改 `Data/I18n/{zh_CN,en}.json` 加 9 个 `ui.character_creation.starter_kit.*` key（title/reset/debug/category 模板/5 个 category name）。**协议影响**：0——`PlayerCreationOptions` 只走进程内 `IGameSessionBackend`，`Protocol.cs` 没碰；不进 `SaveSnapshot`（启动时一次性消费写入 `player.Inventory`）。**构建**：`dotnet build mini-rpg.sln` 0 错 0 警。**测试**：`StarterKit` 过滤 16/16 通过；`MapGen|GameSession|FaceCustomization|PresetScenario|Inventory|Spawn` 回归 117/117 通过；全集（除其他进程归档的 PortraitComposer + FoodConsumeFreshness 2 个 pre-existing 失败）1398/1398 全绿。**未做 / 后续可加**：summary 行没拼"X 件起始物品"摘要文本（要新加 i18n key + 模板，下一轮 polish）；debug 模式没做"全量物品挑选浮窗"（仅放开类上限 + 白名单内自由加），后续如果 QA 需求强烈再做；联机服务端 backend 接到 PlayerCreationOptions 时的 server-side validation 当前没改（联机入口走 LocalSessionBackend → GameSessionModule 直传，未来如果接 ENet 流程需在协议层加 `StartingItemsDebugOverride=false` 强制服务端校验）。

- qtwx-mcp-3-drag-feedback | 2026-04-20 完成（2 个 commit）。**commit eed325a6 feat(inventory)**：背包拖拽拒绝时把"为什么不行"显式告诉玩家。`Module/Panel/InventoryGridPanelModule.cs` 复用现有 HintBar Label 做拖拽 hover hint——正常态白灰色显示默认提示，`_CanDropData` 实时返回 false 时切换成红色 + 具体拒绝原因，可放置时立刻恢复默认；`Refresh()` 末尾统一 `ClearDragHint()` 兜底。网格拒绝原因细分：算 anchor 是否越界（含旋转后仍越界）选 OutOfBounds、否则 OverlapsExisting、data/item/grid 缺失走 ItemNotFound/GridNotFound，全部复用 `log.inventory.grid_error.*` 现有 6 条 i18n。装备 strip 拒绝原因细分：item 不可装备 / BodyPart 不匹配 / Layer 不匹配 各走一条带 fallback 文案的 hint，i18n key `ui.inventory.cannot_equip_*` 暂未在 zh_CN/en.json 落地（避开 `qtwx-mcp-1-revive-facility` 的 i18n WIP）。同步 `Docs/界面与面板.md::4.2 InventoryPanel` 反馈表两条拖拽行 + 反馈节奏契约多写一条 "Refresh 末尾必须 ClearDragHint 兜底"。**commit 85e7ef07 chore(audit)**：预防性巡检全仓 grep `_GetDragData|_CanDropData|_DropData|SetDragForwarding`，确认整个仓库只有 `Module/Panel/InventoryGridPanelModule.cs` 一个文件用了 Godot 4 drag-drop API，其他面板全是 click-based（ListPanelBase 用 Button.Pressed 走 release；其余 GuiInput 都只响应右键/选中），同款"press → Refresh → drag 启动失败"bug 当前不存在于其他面板；落 `Artifacts/panel_press_refresh_audit_2026-04-20.md` 只读报告 + 给未来 drag-drop 面板留 3 条契约 + 1 条可选反射守护测试建议。**协作事故复盘**：第一次 commit (`7101372e` 已 reset) 因 stage 区残留 `qtwx-mcp-1-death-sequence` 的 11 个文件被一并提交，立即 `git reset --soft HEAD~1` + `git restore --staged .` 撤回，重新精确 stage 只 2 个属于本进程的文件后干净落地——后续提交务必 `git diff --cached --stat` 验过再 commit，AGENTS.md "不裹别人 WIP" 这条要更严格执行。**不动**别人在动的 InlineBillBar / 角色创建 / Revival / Demographics / Data / DeathSequenceOverlay 等。`dotnet build MiniRPG.csproj` 0 警告 0 错误。决策标尺 0：Y（让"为什么不能放"成为玩家肉眼可见的反馈，直接推进 Tarkov 风背包可玩闭环；同时是"玩家操作+反馈系统化"模板的实战示范）。
- qtwx-mcp-1-death-sequence | 2026-04-20 完成。仪式感第 1 条 UX 层（焦点死亡演出）落地。决策标尺 0：**Y**（玩家死亡 / 焦点切换 / 全队覆灭从"瞬切+一行日志"升级到"减饱和+大字 heading+镜头缓动"——把"结果可追溯"的代入感从数据 / 日志层正式推到屏幕演出层；mcp9 phase2 + mcp10 都点名了这条缺口）。**改动**：①新增 `App/RuntimeUi/DeathSequenceOverlay.cs`（FullRect `ColorRect` 黑色 modulate fade-in 0.25s → hold → fade-out 0.25s，默认 1.5s/全队 2.4s，`IsActive` 给 UiMode 用作输入抑制门控）；②`App/RuntimeUi/RuntimeCameraController.cs` 加 `BeginCinematicSweep(fromX,fromY,fromZ,duration)` + `TickCinematicSweep(delta)`（FollowActor 模式下 `BuildSnapshot` 在缓动期把 center 在 from→ResolveActiveActorPosition 之间按 ease-out cubic 插值，缓动结束自动复位；`ResetForSession` 同步 `CancelCinematicSweep`）；③`Module/Panel/ToastOverlay.cs` 加 `ToastKind.Heading` + `ShowHeading(text, duration=3s)`（字号 44，白色描边，最小宽度 720px，仍走相同 fade in/out + MaxVisible 队列）；④`App/RuntimeUi/PlayerDeathPresenter.cs` 加 `PresentSequence(kind, actorName?, fromCell?)` + `DeathSequenceKind` 4 值（PartyMemberLost / FocusSwept / PartyWiped / NonActiveMemberLost），构造函数加 3 个可选委托（headingToast / overlay / cameraSweep），不动 `Present` 旧路径；⑤`App/RuntimeUi/GameEventPresentationRouter.cs` 加 `_deathPresenter` 可选注入，分发 `party_member_lost(was_active)` → PartyMemberLost（heading + overlay，缓动留给后一条 active_actor_switched），`active_actor_switched` → FocusSwept（用 `_state.PlayerX/Y/Z` 旧焦点位置触发镜头缓动，再 FlushMap 同步到新 active），`party_wiped` → PartyWiped（更长 overlay + 全队覆灭 heading）然后再调 `_handlePlayerDeath`；⑥`App/Main.Startup.cs` 把 `DeathSequenceOverlay` 加到 `_overlayLayer`，注入 presenter 三个委托（cameraSweep 调 `_runtimeCameraController.BeginCinematicSweep` 后立刻 `FlushMap`），presenter 实例传给 router；⑦`App/Main.cs` `_Process` 加 `_runtimeCameraController?.TickCinematicSweep(delta)` + `IsCinematicSweepActive` 时强制 `FlushMap` 让相机每帧平滑插值；⑧`App/Main.UiMode.cs` `BlocksGameplayInput` + `PausesGameplayLoop` OR 上 `_deathSequenceOverlay.IsActive`（演出 1.5s 内屏蔽玩法输入，避免误操作；`SuppressHudAndAlerts` 不加避免 toast/HUD 也被吞）；⑨`Data/I18n/{en,zh_CN}.json` 补 `death.headline.actor_fell` / `death.headline.party_wiped` / `timeline.lock.death_sequence` 3 key 中英对照（"X 倒下了" / "全队覆灭" / "Death sequence in progress…"）。**不动**：mcp9 phase2 已落地的 heading 日志（HandlePlayerDeath 内 `_playerDeathPresenter.Present` 调用位置不变）；mcp10 残魂掉落；任何 Tools/banana / Conversations / 角色定制 / GridInventory / FoodFreshness 路径。**构建**：`dotnet build MiniRPG.csproj` 0 错 0 警。**测试**：`GameEventPresentationRouter|RuntimeCamera|PlayerDeath|DeathReport|ActiveActorDeath|Timeline` 过滤后 107/107 通过；`LocalizationCatalogTests` 5/5 通过。**未做**：实际 Godot Engine 中的手动验证留给运行时确认（编辑器跑通需要 GUI），逻辑层全绿。

<!--
本节定期清空，避免遮住"现在没人在动什么"。
旧条目按归档日期挪到 Artifacts/wip_archive_<日期>.md，需追溯历史归因时按需查阅。
最近一次归档：[Artifacts/wip_archive_2026-04-19.md](wip_archive_2026-04-19.md)
（覆盖 2026-04-17 至 2026-04-19 全部已完成条目）
-->

- qtwx-mcp-1-vision-soul | 2026-04-20 完成。在 `Docs/产品愿景.md` 的"死亡与复活"之后、"决策标尺"之前新增"**灵魂（扩展目标）**"一节（+62 行），明确为扩展目标、未落地，配合未来 `Soul` 解耦 refactor 做指北。决策标尺 0：**N**（本改动不直接推进"最终可玩"的闭环，属于愿景北极星的扩展——但"灵魂"是"死亡不是终局"的自然延伸，现在写清楚边界是为了等**现有 4 条死亡仪式感 + 复活入口**闭环完成后、再推 Soul 解耦时避免偏离方向；不写死方向，后面任何 AI 都会把它演成 MMO 或 JRPG）。**内容**：①核心抽象——`Soul` 从 `Actor` 解耦为一等对象（承载 `SoulId` / `Personality` / `MemoryLedger` / `SkillImprints` / `RevivalCount` / `ScarLevel`），`Actor` 退化为 Body（`Species` / `Gender` / `Age` / `BaseAttributes` + `CurrentSoulId?`）；②三种跨身体操作表（复活 / 夺舍 / 轮回 / 分身，各列硬代价 + 旁观反应）；③与 `RelationshipModule` / `ActorMemoryModule` / `RumorBus` / 多角色单焦点 / 死亡仪式感的互锁；④5 条非目标（不升天 / 分身≠派系 / NPC 也可被夺舍 / 不灵魂全知 / 不绕 `ServerActionGateway`）；⑤落地顺序（`Soul` 解耦 → 夺舍 → 轮回 → 分身）；⑥4 条验收条件（可追溯 / 连续性 / 代价可见 / 边界守门）。**引用雏形**：`Actor.RevivalCount` / `RevivalCostModel.IsPermanentlyLost` / `ItemCorpseMetadata.SourceActorId` / `soul_fragment` 物品（`Data/items.json` + `Data/item_world_render.json` + i18n）。**不动**：不改 `Docs/开发约定.md` / `Docs/重构路线图.md` / `Docs/涌现世界路线图.md`（仅愿景增目标，其它文档在 Soul 真落地时再动）；不改代码、不加新接口。**构建 / 测试**：纯 Docs 改动，无需跑。
- cursor-opus-mcp5-fx-p1 | 2026-04-20 完成。素材需求清单《素材需求清单_2026-04-20.md》§3 B1 战斗特效 7 张全部落地：**决策标尺 0：Y**（P0 清单里"已被 `Data/combat_fx.json` 引用、替换后立刻进游戏"的一档——`combat_fx.json` 5 条 fx 直接引用 `fx_arrow_projectile/buff_flash/hit_blunt/poison_spit/slash_arc`，把战斗反馈从 820-2664 字节的色块占位推到 RGBA 256×256 半写实贴图，对可玩闭环直接加分）。**产物**：`Assets/Art/Placeholders/effects/fx_{arrow_projectile,buff_flash,hit_blunt,lightning_strike,pickup_glint,poison_spit,slash_arc}.png` 全部 **RGBA 256×256** 23-57 KB、`.png.import` 沿用不覆盖；源图 + rembg 中间产物保留在 `Artifacts/preview/b1_effects/` 作为 B1 风格锚点（与 `Artifacts/preview_props_2026-04-19/` B0 同规格）。**流程**：`banana_gen.py --style style_base --subject <E01-E07> --negatives standard` → `rembg_cutout.py`（u2net 抠图）→ `crop_square_resize.py --size 256 --margin 0.06`，prompt 直接复用《banana资源生成提示词总表_2026-04-18.md》§11 E01-E07 现货，未做二次中文翻译。**顺带**：`Tools/banana/banana_gen.py::_openai_chat_completion` 加**指数退避自动重试**（捕获 `http.client.IncompleteRead` / `urllib.error.URLError` / `TimeoutError` / `ConnectionError` + 5xx `HTTPError`；默认 3 次 retry、基数 3 秒；4xx 不重试；不改签名、不影响 gemini 原生协议路径）——原因是中转 `sg2c.dchai.cn` 的 `Nano_Banana_2_2K_0` 流式返回 4MB 图常被 `IncompleteRead` 或 Cloudflare 524 切断，7 张里最难一张 `fx_buff_flash` 靠重试 766s 才成功。**不动**：`Data/combat_fx.json` 与 `Module/Render/CombatFxPlayer.cs`（后者是 mcp8 M7 "重复 Id warning" 的范围，只换 png 对它无影响）。**构建**：`dotnet build MiniRPG.csproj` 0 错 0 警（实际没触碰 .cs / .csproj / .json，仅换 png）。**成本**：约 $0.47 估算（官方 Gemini 价目，中转实收按渠道）。
- qtwx-mcp-3-inv-drag-fix | 2026-04-20 完成（2 个 commit）。决策标尺 0：Y。**commit 576cbfdf**：①`Module/Panel/InventoryGridPanelModule.cs` 修背包网格 + 装备 strip 左键拖不动双 bug——placement 从 `Control` 换成 `DragDropControl` 自己注册 `OnGetDragData/OnCanDrop/OnDrop`（Godot 4 的 `_GetDragData` 不会向父节点冒泡，原代码注释的"冒泡到父 canvas 启动拖拽"是 Godot 3 心智模型错迁）、`OnPlacementGuiInput`/`OnEquipStripGuiInput` 的 Pressed 分支不再立刻 `Refresh()` 推迟到 Released（按下就 `Refresh()` 会 `QueueFree` 当前源 Control，drag threshold 还没到源就被销毁，装备 strip 本来是 `DragDropControl` 也因此拖不动），顺手清 `SetDragForwarding` 残留注释；②`Docs/界面与面板.md` 第 4 节引入「操作 & 反馈」5 栏模板（输入 / 即时反馈 / 命令 / 失败 i18n / 跨面板联动），4.2 InventoryPanel 按模板完整填实作为基线范例，4.3 SkillBar / 4.5 ChestPanel / 4.6 TradePanel / 4.7 ConversationPanel / 4.9 ActorInspectPanel 加 TODO 占位（按 AGENTS.md 触发式维护等真正改到再填）；③`Module/Panel/README.md` 新增「Godot 4 C# 面板实现常识」段——drag-drop 三条坑（源必须是 `DragDropControl`、不用 `SetDragForwarding+Callable.From`、`MouseFilter` 选择）+ Refresh 节奏契约 + 反馈可观察性要求，把"今天这个坑"写成下一位面板作者的基线常识而不是散落在 `*.cs` 内联注释。**commit 70b83fc0**：`Artifacts/scan_2026_04_19/` 目录 9 份分区底稿移到 `_sources/` 子目录存档溯源，汇总稿留顶层作唯一入口（按 AGENTS.md "下次改代码时还值不值得读" 的判断，分区底稿内容已被汇总吃掉）。**不动** 别人在动的 `Module/LogModule.cs` / `Revival/*` / `Demographics/*` / `Data/thoughts.json` / `Data/items.json`；**不动** 其他进程遗留的 `App/Main.*.cs` / `Module/Panel/*` 大面积 M 文件。`dotnet build MiniRPG.csproj` 0 警告 0 错误。
- qtwx-mcp-7-orphan-cleanup | 2026-04-20 完成。孤儿代码清理（承接 `Artifacts/全量巡检_2026-04-20.md` 之后的独立巡检）。决策标尺 0：**Y**（删 -33KB 死码后，下一位 AI 读 `Docs/架构现状.md` 边界列表时不被"刚写完就被绕过的 `SurfaceFreeMoveTraversal`"、"0 caller 的 `NoopLocalServerLauncher`"、"错挂 `JsonDerivedType` 的 `ITagSourcePolymorphic`"、"Dialog 旧系统残留 `DialogSet/Entry/Option`"等误导，每减噪一分就减少一分错改风险）。**⚠ 已代 `cursor-opus-mcp9-audit-phase2` 做完 `SurfaceFreeMoveTraversal.cs` 的"删或接入"决断——选"删"**（全仓 0 代码 caller，注释声称"给 ActionModule 和 AutoNavigationPlanner 共用"但两者均未接入）；`Data/Config/demographics_runtime.json` 的决断未动，仍留给 mcp9。改动：①**删整文件 7 个** —— `MiniRPG.Shared/Core/World/SurfaceFreeMoveTraversal.cs`、`Module/Network/NoopLocalServerLauncher.cs`（运行时统一 `LocalProcessServerLauncher`）、`Module/Panel/PanelTransition.cs`（UI helper 无 using）、`MiniRPG.Shared/Core/World/BlockPlaceModule.cs` + `Tests/MiniRPG.Tests/PlacementConnectivityTests.cs`（建造现走 `FacilityConstructionModule`）、`MiniRPG.Shared/Core/Event/DailySummaryBuilder.cs` + `Tests/MiniRPG.Tests/DailySummaryBuilderTests.cs`（`DailySummary` 对象无 HUD/面板消费者）；②**文件内删类** —— `PresetDB.cs` 删 `private interface IHasId`（`GetId` 用反射读 "Id"）、`TagSystem.cs` 删 `ITagSourcePolymorphic` + 5 个错挂的 `[JsonDerivedType]`（挂在无实现子接口，System.Text.Json 永读不到）、`DialogTypes.cs` 删 `DialogSet/Entry/Option`（保留 `DialogCondition`/`DialogEffect`，仍被 `ConversationModule:240` + `DialogConditionMatcher` 使用）；③**轻改注释** `CalendarServiceTests.cs` 移除 `DailySummaryBuilder` 的 `<see cref>` 引用。**构建**：`dotnet build MiniRPG.csproj` 0 错 0 警。**测试**：过滤 `PortraitComposer` / `FoodConsumeFreshness`（归档已记录的 pre-existing native crash / 断言漂移，与本轮无关）后 **1379/1379 通过**。完整扫描数据落盘 `Artifacts/orphan_scan_raw.csv`（1049 个类型的全仓引用计数），供后续增量扫描复用。
- qtwx-mcp-10-死亡仪式感3+4 | 2026-04-20 完成。补愿景《产品愿景》死亡-复活段仪式感要求**第 3 条（旁观反应）+ 第 4 条（失败产出）**。决策标尺 0：Y——死亡场景的代入感直接增强，不再是沉默的 `ok=false`。**第 3 条（旁观挂 thought）**：新增 `MiniRPG.Shared/Core/Social/BystanderGriefCapturer.cs`（IGameEventConsequenceHandler，同 Z 层 Chebyshev<=8 扫描旁观者，排除受害者/凶手/已死/亲属 KinGriefTargets 中的 survivor，按是否同 Party 发 `ally_died` 或 `witnessed_death` thought）；`Data/thoughts.json` +`witnessed_death` -6/240 + `ally_died` -10/480；`Data/I18n/{en,zh_CN}.json` 补两 key（中："目睹了一场死亡" / "同伴倒下了"；英："Witnessed a death" / "A companion fell"）；`App/Main.Startup.cs` 在 KinshipLossCapturer 后 Register；`MiniRPG.Shared/Core/Multiplayer/ServerSideConsequenceDispatcher.cs` 对称加 `BystanderGrief` 属性 + Register；新增 `Tests/MiniRPG.Tests/BystanderGriefCapturerTests.cs` 7 case（范围 / 跨 Z / 凶手豁免 / 亲属优先 / 非死亡 / 3 种 health death cause / 死者豁免）；HandlerCount 5→6 同步 `ServerSideConsequenceDispatcherTests` 与 `RoomRuntimeHostConsequencePipelineTests`。**第 4 条（失败产出"残魂"）**：现状盘点发现复活施法多回合化（第 2 条）已由 `TimelineTurnManager.Revive`+`RevivalRitualBuffId` 其他进程先行完成；失败文本 7 种 FailureReason 的 LogModule 翻译也齐全；唯一缺的是愿景字面指定的**"'残魂'物品、永久损坏的灵魂石"产出**。补：`Data/items.json` 新增 `soul_fragment` 物品（misc 类，0.1 重量，tags `{灵魂,珍稀}`）；`Data/item_world_render.json` 补 world render mapping（catalog_texture + 0.45 scale）；`Data/I18n/{en,zh_CN}.json` 补 `data.item.soul_fragment.name`；`MiniRPG.Shared/Core/Revival/ReviveService.cs` 扩 `SoulFragmentItemId` 常量 + `Failure(..., dropSoulFragment)` 分支：`PermanentlyLost` / `SpellMisfire` 时 `PresetDB.CloneItem` → `state.World.PlaceItem(reviver.X, reviver.Y, reviver.Z, shard)` 并发第二条 `revival_soul_fragment_dropped` 事件；`Module/LogModule.cs` 加该事件翻译（中："仪式的残响凝成了一块{item}，掉在{initiator}脚下。"；英："A {item} crystallizes from the failed ritual at {initiator}'s feet."）；`Tests/MiniRPG.Tests/ReviveServiceTests.cs` 新增 3 case（PermanentlyLost 掉残魂 / Misfire 掉残魂+扣半材料 / InsufficientMaterials 不掉——前置检查不产出防 farming）。**未做**：仪式感第 1 条（焦点死亡 UI 演出：减饱和 overlay + 大字弹幕 + 镜头缓动切焦点）属 C 赛道 Panel/UI，等独占 C 赛道的通道接手；FlushMap 相机驱动也需要拆 `SyncViewToActiveActor` 才能做缓动。**构建**：`dotnet build mini-rpg.sln` 0 错 0 警。**测试**：过滤 PortraitComposer/FoodConsumeFreshness 两项预先失败后，全量 1382/1382 通过；其中新增 7 (Bystander) + 3 (Revive soul fragment) 都绿。
- cursor-opus-mcp9-audit-phase2 | 巡检 phase2（H1 + M9 + 愿景仪式感 3/4 补接线）。决策标尺 0：Y（**愿景仪式感第 1-4 条全部至少数据层 + 日志层兑现**——玩家现在能在死亡-复活流程里看到：①死亡切换演出 heading（phase1）、②仪式按回合推进 + 剩余回合日志、③旁观 NPC 自动获得 thought（mcp10 落地，本轮确认）、④复活失败落残魂 + toast + 日志。这直接把"结果可追溯 + 代入感"从数据层推到 UX 层；往可玩闭环近一步）。**本轮改动**：①`MiniRPG.Shared/Core/Combat/TimelineTurnManager.cs::TryExecuteRevive` 从 `corpse.Corpse?.SourceActorId` 查回原 actor，给 `revival_ritual_started` / `revival_ritual_progress` 事件调 `IdentificationModule.PopulateTargetIdentity` 填 {target}；②`Module/LogModule.cs` 新增 `revival_ritual_started` / `revival_ritual_progress` switch 分支（用已存在的 `log.revival.ritual_started` / `log.revival.ritual_progress` i18n key）；③`Data/I18n/{zh_CN,en}.json` 补 `log.revival.soul_fragment_dropped` + `toast.revival_soul_fragment_dropped` 两条 key（填 LogModule 已引用但 i18n 缺失的孤儿）；④`App/RuntimeUi/GameEventPresentationRouter.cs` 加 `case "revival_soul_fragment_dropped"`，调 `_ground.Invalidate()` + 发一条 warning toast 让玩家立刻看到"一块 X 掉落"；⑤**删**孤儿文件 `Data/Config/demographics_runtime.json`（0 caller，字段与 `conception_defaults.json` 重叠但值不一致——保留只会让运维改错文件，不读的文件先删干净）。**依赖 / 协作确认**：H1 `revival_failed` + `conversation_opened/closed/observer_joined/takeover_*` 翻译已由其他进程在 LogModule 落地（`Ardynox` / 另一 cursor-opus instance，非我），`revival_soul_fragment_dropped` switch 分支也是其他进程加的（未提交），本轮只补 i18n key + toast。M1 `SurfaceFreeMoveTraversal.cs` 已由 `qtwx-mcp-7-orphan-cleanup` 删除；愿景仪式感第 3 条（旁观反应 thought）已由 `qtwx-mcp-10-bystander-grief` 完整落地；愿景仪式感第 4 条（失败残魂 item drop）已由其他进程在 `ReviveService.cs` 落地 + 加 3 个覆盖测试（`TryRevive_PermanentlyLost_WithWorld_DropsSoulFragmentAtReviverFeet` / `TryRevive_SpellMisfire_WithWorld_DropsSoulFragmentAndConsumesHalfMaterials` / `TryRevive_InsufficientMaterials_DoesNotDropSoulFragment`）。**构建**：`dotnet build mini-rpg.sln` 0 错 0 警。**测试**：`Revive|DeathReport|PlayerDeathPresenter|Bystander|TimelineTurn` 过滤 59/59 通过。

- cursor-opus-mcp9-audit-patch | `Artifacts/全量巡检_2026-04-20.md` 速通清单 6 条已清（+ Gemini 文案）：决策标尺 0：Y（**H2 + Δ-8** 直接让中文玩家死亡报告面板从"英文 fallback + 双 heading"恢复到"中文标准 + 单 heading"，兑现愿景"结果可追溯"的核心 UX；**H5** 让 AutoTest 冒烟在 Tab 切焦点后断言正确、保护 CI 基座；**M11 + L4/L5** 清编译警告和文档漂移，降低下一位协作者的认知成本）。具体改动：①`Data/I18n/zh_CN.json` + `en.json` 补 10 条 death.* key（death.party_wiped / death.report.cause / death.report.recent_count / death.cause.{killed_by,killed,incapacitated,blood_loss,infection,food_poisoning,unknown}），文案经 Gemini subagent 审美打磨（中文去翻译腔："战斗毙命" / "感染身亡" / "全队覆灭"，英文 PZ clinical 风）；②`App/Main.Timeline.cs::HandlePlayerDeath` 删除 Main 内的 heading 日志（原本用 ternary 只分 killed/incapacitated，party_wiped 落回 fallback "death.killed"），把 `_playerDeathPresenter.Present` 调用上移到 turn/floor/kills 详情行之前，让 Presenter 统一写 heading + 死因 + 最近事件计数；③`App/Main.AutoTest.cs::CaptureAutoTestSnapshot` `ActorModule.GetPlayer(_state)` → `ActiveActorAccess.GetActive(_state)`，groundItemCount 一起切到 active actor 的坐标（Tab 切焦点后断言指向当前受控角色而非队长）；④`Tests/MiniRPG.Tests/DemographicsBabyFoodTests.cs` 4 处 `default` → `default!`（消除 CS8625）；⑤`Data/README.md` 删 `dialogs.json`、改写为 `Conversations/*.conversation.json`；⑥`Docs/会话生命周期.md` `_dialogPanel` → `_conversationPanel`。**跳过**：M4（审计误报——`Scene/DialogPanel.tscn` 被 `ConversationPanelModule` 复用，`App/Main.Panels.cs:45` 载入此 scene 作为 ConversationPanel 骨架，非死资源）。**构建**：`dotnet build mini-rpg.sln` 0 错误 0 警告（上次 4 条 CS8625 已清）。**测试**：`DeathReport/PlayerDeathPresenter/Revive/Demographics` 过滤后 73/73 通过；整套 1117 个测试中有 2 个预先失败（`FoodConsumeFreshnessTests.TryConsumeFood_Rotten...` 断言漂移 20 vs 12.2、`PortraitComposerTests.Clear_Purges...` Godot.Image 原生崩溃 AccessViolation），均属于其他进程 untracked 新测试，不在本轮改动范围内。
