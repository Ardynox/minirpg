# B7 · surgery / room / capacity / limb（style lock）

继承 `Artifacts/style_locks_B0.md`。`ui_icons/` 内对应前缀已齐；`limb_*` 实际文件名以 `PlaceholderUiIconCatalog` 为准（见 `Artifacts/素材身份卡_运行时映射补充_2026-04-21.md`）。

**发包**：教材示意图 vs 血腥暗示等按身份卡 B7 清单问 Gemini 后补「批复」段。
