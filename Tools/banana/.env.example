# 复制本文件为 Tools/banana/.env 后填入真实 key
# .env 已在 .gitignore，绝对不会进 git

# 申请 key：https://aistudio.google.com/apikey
GEMINI_API_KEY=

# OpenAI 兼容中转（xais 等）时设置；走 Google 原生 gemini 协议则留空或 gemini
# GEMINI_PROTOCOL=openai

# === 可选：第三方中转站 / 自定义 endpoint ===
# 如果走官方 Google API，留空即可。
# 如果走第三方中转（如 suxi / one-api 等兼容 Gemini 原生协议的网关），填它的根域名：
# GEMINI_BASE_URL=https://new.suxi.ai
#
# 注意：
# - GEMINI_API_KEY 要换成中转站给的 key（通常是 sk-... 开头，不是 Google 的 AIza...）
# - 中转站能看到你全部 prompt 和生成内容，合规的资源可以用，敏感内容别发
# - 走国内中转时通常不需要代理，看下面代理段可以全部留空

# === 代理（走 Google 官方 API 时中国大陆必填）===
# Gemini API endpoint 在中国大陆直连不通，需要本地代理转发。
# 浏览器插件代理（SwitchyOmega 等）只对浏览器生效，Python 进程必须用环境变量。
# 把下面任一行改成你本地代理实际端口（取消注释）：
#
# Clash 默认：
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
#
# v2rayN 默认：
# HTTPS_PROXY=http://127.0.0.1:10809
# HTTP_PROXY=http://127.0.0.1:10809
#
# Shadowsocks 默认 SOCKS5：
# HTTPS_PROXY=socks5h://127.0.0.1:1080
# HTTP_PROXY=socks5h://127.0.0.1:1080
#
# 如果不知道端口：打开你常用的代理客户端，看「端口设置」或「Listen Port」。

# === rembg（rembg_cutout.py）模型目录 ===
# 默认：%USERPROFILE%\.u2net\（内含 u2net.onnx）。手动下载见 Tools/banana/README.md §7。
# 若模型不在默认位置，取消下一行并改成你的目录（不要指向 .onnx 文件本身）：
# U2NET_HOME=C:\Users\你的用户名\.u2net
