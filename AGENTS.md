# AGENTS.md

本仓库协作原则：务实。
目标是让后续 AI 用最少上下文完成当前任务，不做全仓扫描，不重复已有文档。

## 先读什么

- 先读 `Docs/README.md`。
- 如果任务明确落在某个复杂目录，先看该目录下的 `README.md`，再按需读代码。
- 默认只在这些主目录里找代码和配置：`App/`、`MiniRPG.Shared/`、`Module/`、`Data/`、`Tools/`、`Tests/`。
- 按任务类型只补读 1 到 2 份：
  - 入口、边界、主流程：`Docs/架构现状.md`
  - 默认策略、协作规则、文档规则：`Docs/开发约定.md`
  - 重构目标、批次、完成标准：`Docs/重构路线图.md`
  - 构建、测试、导出、验收：`Docs/运行与测试.md`
- 如果任务已经明确落在某个目录，优先读局部入口和相关代码；不要先做全仓目录树或全量调用链展开。

## 不要默认读什么

- `Artifacts/` 下的一次性报告
- `.claude/worktrees/`、`.codex_tmp/`、`.codex/`、`.cursor/` 下的历史产物或临时素材
- `.godot/`、`bin/`、`obj/`、`.ruff_cache/`、`obj-gen/`、`Build/` 等生成物或缓存
- `build-diag.txt` 这类大体积诊断输出
- 日期型审计、扫描、占位设计稿

只有在任务明确要求时，才读取这些内容。
如果搜索结果先命中这些路径，先收紧搜索范围，不要继续展开。

## 文档规则

- `Docs/` 只放长期维护、直接服务开发决策的文档。
- 一份文档只回答一个稳定问题，不写大而全总览。
- 局部 `README` 只做触发式补充：某个目录反复成为高上下文热点时才新增，不追求铺满所有目录。
- 不生成这些长期文档：
  - 日期型审计或扫描
  - 全量 public API
  - 全量调用链
  - 全量目录树
  - 没有收敛边界的 TODO 文档
- 一次性分析、实验记录、巡检结果放 `Artifacts/`。

## 修改时的默认策略

- 先复用现有入口和边界，再考虑新增抽象。
- 小改动优先局部修补，不为了“整洁”扩大重构范围。
- 如果一个文件已经成为高频入口且明显过胖，优先拆一小步职责，再考虑补更多解释文档。
- 只有当默认策略、稳定边界、重构目标、运行命令发生变化时，才同步更新 `Docs/`。
- 更新文档时，只改和本次任务直接相关的那一份或两份，不整套重写。

## 对 AI 的要求

- 先定位，再动手；先读必要上下文，再改代码。
- 回答和改动都尽量引用现有事实，不把历史草稿当当前事实。
- 产出以“帮助下一次快速定位、做决定、避免误改”为准；做不到这一点的内容不要留下。


## 决策标尺（必须回答）

任何 commit / PR 描述都要回答 `Docs/产品愿景.md` 的**决策标尺第 0 条**：
"这个改动离最终可玩更近了吗？" —— 至少给出 Y/N + 一句话理由。
如果答 N（本改动不直接推进可玩闭环），要在描述里说明为何优先这一步（例如：修编译阻塞 / 别人依赖 / 技术债必须还）。

## 多进程协作纪律

本仓库常同时有多个 AI / 进程并行修改。为减少互相覆盖与 Tests 编译被打断：

- 开工前先看 `Artifacts/wip.md`（若存在）里别人正在动的文件清单；不主动碰不属于你这块的文件。
- 改动热点文件（`App/Main*.cs`、`Module/Panel/*`、`Module/Render/IsometricVoxelRenderer*`、`Data/I18n/*`）前，先在 `Artifacts/wip.md` 登记一行"`<进程名>` 在动 `<路径>`"。
- 若必须跨区动别人在改的文件，改动前先在回复里明确说"可能会撞 XX 进程的 YY 改动"，让调度者（用户）决定。
- 非必要不动 `Tests/`：接口/签名变化由改接口的进程同步修相关 fake。