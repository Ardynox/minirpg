# AGENTS.md

本仓库协作原则：务实。
目标是让后续 AI 用最少上下文完成当前任务，不做全仓扫描，不重复已有文档。

## 先读什么

- 先读 `Docs/README.md`。
- 按任务类型只补读 1 到 2 份：
  - 入口、边界、主流程：`Docs/架构现状.md`
  - 默认策略、协作规则、文档规则：`Docs/开发约定.md`
  - 重构目标、批次、完成标准：`Docs/重构路线图.md`
  - 构建、测试、导出、验收：`Docs/运行与测试.md`
- 如果任务已经明确落在某个目录，优先直接读相关代码；不要先做全仓目录树或全量调用链展开。

## 不要默认读什么

- `Artifacts/` 下的一次性报告
- `.claude/`、`.codex/`、`.cursor/` 下的历史产物
- `.godot/`、`.ruff_cache/`、`obj-gen/` 等生成物或缓存
- `build-diag.txt` 这类大体积诊断输出
- 日期型审计、扫描、占位设计稿

只有在任务明确要求时，才读取这些内容。

## 文档规则

- `Docs/` 只放长期维护、直接服务开发决策的文档。
- 一份文档只回答一个稳定问题，不写大而全总览。
- 不生成这些长期文档：
  - 日期型审计或扫描
  - 全量 public API
  - 全量调用链
  - 全量目录树
  - 没有收敛边界的 TODO 文档
- 一次性分析、实验记录、巡检结果放 `Artifacts/`。

## 修改时的默认策略

- 先复用现有入口和边界，再考虑新增抽象。
- 小改动优先局部修补，不为了“整洁”扩大重构范围。
- 只有当默认策略、稳定边界、重构目标、运行命令发生变化时，才同步更新 `Docs/`。
- 更新文档时，只改和本次任务直接相关的那一份或两份，不整套重写。

## 对 AI 的要求

- 先定位，再动手；先读必要上下文，再改代码。
- 回答和改动都尽量引用现有事实，不把历史草稿当当前事实。
- 产出以“帮助下一次快速定位、做决定、避免误改”为准；做不到这一点的内容不要留下。
